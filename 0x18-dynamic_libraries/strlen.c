#include "main.h"

/**
 *_strlen - calculate the length of a string
 * @s: the string to measur
 * Return: the string lengh
 */

int _strlen(char *s)
{
	int len = 0;

	while (*s++)

	++len;

	return (len);

}
